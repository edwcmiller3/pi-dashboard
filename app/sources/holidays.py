"""Holidays source — `holidays` package.

Phase 1 stub. Phase 5 implements: actual-date holidays only, with the lesser
tier + DST markers (v1 scope, expanded 2026-06-28). US holidays.
"""

from __future__ import annotations

from typing import Any


def get_holidays(year: int) -> list[dict[str, Any]]:
    """Return holidays/markers for `year`. (stub)"""
    raise NotImplementedError("holidays.get_holidays — implemented in Phase 5")
